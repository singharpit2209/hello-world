name: hello-api-node

on:
  push:
    paths:
      - "hello-api-node/**"
      - ".github/workflows/hello-api-node.yml"
  workflow_dispatch:
    inputs:
      deploy:
        description: Deploy to Lambda after build
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: singharpit2209/shared-workflows/.github/workflows/build-node.yml@main
    with:
      working_directory: hello-api-node

  deploy:
    needs: build
    if: github.event_name == 'workflow_dispatch' && inputs.deploy
    uses: singharpit2209/shared-workflows/.github/workflows/deploy-lambda.yml@main
    with:
      artifact_version: ${{ needs.build.outputs.artifact_version }}
      artifact_name: ${{ needs.build.outputs.artifact_name }}
      artifact_type: ${{ needs.build.outputs.artifact_type }}
      build_artifact_name: node-lambda-${{ needs.build.outputs.artifact_version }}
      lambda_function_name: ${{ vars.HELLO_API_NODE_LAMBDA_FUNCTION_NAME }}
      artifact_bucket_name: ${{ vars.HELLO_API_NODE_ARTIFACT_BUCKET }}
      lambda_alias_name: live
      environment: dev
      aws_role_arn: ${{ vars.AWS_ROLE_ARN }}
      aws_region: ${{ vars.AWS_REGION }}
    secrets: inherit
