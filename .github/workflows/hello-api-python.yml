name: hello-api-python

on:
  push:
    paths:
      - "hello-api-python/**"
      - ".github/workflows/hello-api-python.yml"
  workflow_dispatch:
    inputs:
      deploy:
        description: Deploy to Lambda after build
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: singharpit2209/shared-workflows/.github/workflows/build-python.yml@main
    with:
      working_directory: hello-api-python

  deploy:
    needs: build
    if: github.event_name == 'workflow_dispatch' && inputs.deploy
    uses: singharpit2209/shared-workflows/.github/workflows/deploy-lambda.yml@main
    with:
      artifact_version: ${{ needs.build.outputs.artifact_version }}
      artifact_name: ${{ needs.build.outputs.artifact_name }}
      artifact_type: ${{ needs.build.outputs.artifact_type }}
      build_artifact_name: python-lambda-${{ needs.build.outputs.artifact_version }}
      lambda_function_name: ${{ vars.HELLO_API_PYTHON_LAMBDA_FUNCTION_NAME }}
      artifact_bucket_name: ${{ vars.HELLO_API_PYTHON_ARTIFACT_BUCKET }}
      lambda_alias_name: live
      environment: dev
      aws_role_arn: ${{ vars.AWS_ROLE_ARN }}
      aws_region: ${{ vars.AWS_REGION }}
    secrets: inherit
